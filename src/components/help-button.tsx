import { Button, Grid } from "@mui/material";

const HelpButton = () => {
  return (
    <Button
      variant="contained"
      style={{
        marginTop: "30px",
        width: "200px",
        height: "50px",
        fontSize: "20pt",
      }}
      onClick={() => {}}
    >
      Help!
    </Button>
  );
};
export default HelpButton;
